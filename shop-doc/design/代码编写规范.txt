每一个表的操作在一个DAO 【定位、复用】
service层每一个表对应一个包（包含一个至多个service）【定位、复用】
每个service只能访问其所在包对应的DAO和其他service（不能直接操作其他DAO）
用枚举类定义状态等常量且最好放在公共包中
尽量使用生成的DAO（少写代码，少出bug，表，字段变更时重新生成，易于维护）

大模块间（order,product）不得依赖及使用事物